# 模型文件规定了权限由sub,obj,act三要素组成 只有在策略列表中有和它完全相同的策略时，该请求才能通过
[request_definition]
r = sub, obj, act

[policy_definition]
p = sub, obj, act

[role_definition]
g = _, _

# some(where (p.eft == allow))表示只要有一条策略允许即可。
[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = g(r.sub, p.sub) == true \
    && keyMatch2(r.obj, p.obj) == true \
    && regexMatch(r.act, p.act) == true \
    || r.sub == "root"